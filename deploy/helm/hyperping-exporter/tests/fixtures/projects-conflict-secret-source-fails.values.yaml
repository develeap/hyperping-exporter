# Multi-project fixture: a single project must declare exactly ONE
# secret source. Setting both inline apiKey AND existingSecret on the
# same project is a configuration conflict and MUST abort the render.
config:
  projects:
    - id: hyp_core
      apiKey: inline-key
      existingSecret: hp-core
