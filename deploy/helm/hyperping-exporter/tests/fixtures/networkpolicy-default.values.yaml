config:
  apiKey: x
# Render-test fixture: enables the chart's default NetworkPolicy explicitly
# (1.5.1 defaults NetworkPolicy off). Asserts the vanilla NP shape: DNS
# egress to kube-dns, TCP/443 egress to 0.0.0.0/0 with the RFC1918 except
# list intact.
networkPolicy:
  enabled: true
