apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: hyperping-prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      httpMethod: POST
      timeInterval: "60s"
