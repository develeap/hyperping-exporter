# Binary output (root only, not deploy/helm/hyperping-exporter/)
/hyperping-exporter
/dist/

# Test & coverage artifacts
coverage.out
coverage.html
*.test

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# goreleaser snapshot
/dist/

# k8s Secret with real credentials (use secret.yaml.example as template)
deploy/k8s/secret.yaml

# Env files with real credentials (use *.example templates)
.env
deploy/.env

# Local flow files
samber-parity.flow.yaml

# Helm chart test artifacts (run-only state, not committed)
deploy/helm/hyperping-exporter/tests/artifacts/
!deploy/helm/hyperping-exporter/tests/artifacts/.gitkeep

# Local planning notes (not committed)
docs/plans/
